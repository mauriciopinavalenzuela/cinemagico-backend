class Reproduccion {
    id: number;
    pelicula: Pelicula;
    fecha: Date;
  
    constructor(id: number, pelicula: Pelicula, fecha: Date) {
      this.id = id;
      this.pelicula = pelicula;
      this.fecha = fecha;
    }
  }
  